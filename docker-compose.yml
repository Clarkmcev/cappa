version: "3.8"
networks:
  caddy:
services:
  caddy:
    container_name: caddy
    build:
      dockerfile: ./docker/caddy/Dockerfile
      context: ./frontend
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
    ports:
      - "80:80"
      - "443:443"
    networks:
      - caddy
