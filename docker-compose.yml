version: "3.8"

services:
  caddy:
    build:
      context: ./frontend
      dockerfile: ./Dockerfile
    container_name: ${STUDIO_FRONTEND_IMAGE}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./frontend/Caddyfile.dev:/etc/caddy/Caddyfile
      - caddy_data:/root/.local/share/caddy
      - caddy_config:/root/.config/caddy
    restart: unless-stopped

volumes:
  caddy_data:
  caddy_config:
